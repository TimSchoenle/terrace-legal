name: Auto-Merge Release PRs

# Approves and merges the release-please pull request once it has sat for the configured time
# with green checks, via the shared reusable workflow from `TimSchoenle/actions`.
#
# `branch_pattern` and `trusted_authors` are what stop an ordinary pull request being swept up by
# the schedule. The delay is the window in which a human can object to a release. Nothing here
# bypasses the required `ci` check.

on:
  schedule:
    - cron: '0 */6 * * *' # every 6 hours
  pull_request_review:
    types: [dismissed]
  workflow_dispatch:
    inputs:
      time_elapsed:
        description: 'Time elapsed since PR creation (e.g. "24h", "1d").'
        required: true
        default: '1d'

permissions: {}

jobs:
  auto-merge-releases:
    name: Auto Merge Releases
    permissions:
      contents: write # to merge PRs
      pull-requests: write # to approve and merge PRs
    uses: TimSchoenle/actions/.github/workflows/maintenance-timed-auto-pr-approve.yaml@dfe326713342286a585aa339e96afaa7d404483f # tag=workflows-maintenance-timed-auto-pr-approve-v1.2.35
    with:
      time_elapsed: ${{ inputs.time_elapsed || '7d' }}
      branch_pattern: 'release-please--branches--%'
      trusted_authors: '253958393,248313786' # actions-maintenance-bot, automatic-release-manager
    secrets:
      app_id: ${{ secrets.ACTIONS_MAINTENANCE_APP_ID }}
      private_key: ${{ secrets.ACTIONS_MAINTENANCE_PRIVATE_KEY }}
